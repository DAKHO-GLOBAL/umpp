import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales
  static const Color primary = Color(0xFF1E6C41); // Vert principal
  static const Color primaryLight = Color(0xFF2A8C54); // Vert clair
  static const Color primaryDark = Color(0xFF154D2E); // Vert foncé

  static const Color secondary = Color(0xFFE0A526); // Jaune/or
  static const Color secondaryLight = Color(0xFFF1C40F); // Jaune clair
  static const Color secondaryDark = Color(0xFFD4980E); // Jaune foncé

  // Couleurs fonctionnelles
  static const Color success = Color(0xFF2ECC71); // Vert succès
  static const Color error = Color(0xFFE74C3C); // Rouge erreur
  static const Color warning = Color(0xFFF39C12); // Orange avertissement
  static const Color info = Color(0xFF3498DB); // Bleu information

  // Couleurs de fond - thème clair
  static const Color lightBackground = Color(0xFFF5F7FA);
  static const Color lightSurface = Color(0xFFFFFFFF);

  // Couleurs de fond - thème sombre
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);

  // Couleurs de texte - thème clair
  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textDisabled = Color(0xFFBDBDBD);

  // Couleurs de texte - thème sombre
  static const Color textPrimaryDark = Color(0xFFFFFFFF);
  static const Color textSecondaryDark = Color(0xFFB0B0B0);
  static const Color textDisabledDark = Color(0xFF6E6E6E);

  // Couleurs utilitaires
  static const Color divider = Color(0xFFE0E0E0);
  static const Color dividerDark = Color(0xFF424242);
  static const Color overlay = Color(0x80000000);
}